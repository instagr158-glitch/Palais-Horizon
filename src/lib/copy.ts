/**
 * Brand constants that never change per language.
 * All translatable copy lives in `src/i18n/*` — use `getServerDict()` in server
 * components and `useI18n()` in client components.
 */
export const BRAND = {
  name: "Palais Horizon",
} as const;
